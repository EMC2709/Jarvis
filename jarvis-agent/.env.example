# Copy this file to .env and fill in your values

# Bearer token the browser must send — change this to something secret
JARVIS_TOKEN=change-me-to-something-secret

# Port to listen on (default 3000)
PORT=3000
